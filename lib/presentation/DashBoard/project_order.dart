import 'package:flutter/material.dart';

class ProjectOrder extends StatelessWidget {
  const ProjectOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Project Order"),),
      body: null,
    );
  }
}
