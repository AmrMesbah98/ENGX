import 'package:flutter/material.dart';

class SupportCourses extends StatelessWidget {
  const SupportCourses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Support Courses"),),
      body: null,
    );
  }
}
